name=input("Enter you name: ")
print("Hello my name is : ", name) ### by default input function takes the input as string and returns the string. So we can directly print the name variable which will contain the string entered by the user.

